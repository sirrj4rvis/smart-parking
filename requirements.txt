# Minimum versions; pip resolves the newest compatible wheels per platform.
# --- Core ---
Flask>=3.0.3
Werkzeug>=3.0.3
python-dotenv>=1.0.1
gunicorn>=22.0.0

# --- Database / ORM / migrations ---
Flask-SQLAlchemy>=3.1.1
Flask-Migrate>=4.0.7
SQLAlchemy>=2.0.36
psycopg[binary]>=3.2.10

# --- Security / auth ---
Flask-WTF>=1.2.1
Flask-Limiter>=3.8.0
Flask-JWT-Extended>=4.6.0

# --- Caching / realtime / background ---
Flask-Caching>=2.3.0
redis>=5.0.7
Flask-SocketIO>=5.3.6
python-socketio>=5.11.3
celery>=5.4.0
# Production WSGI: gevent worker gives true WebSocket support + high connection
# concurrency for Socket.IO (the gthread/threading combo only does long-polling).
gevent>=24.2.1
gevent-websocket>=0.10.1
# Persistent, restart-safe Celery beat schedule (avoids the ephemeral shelve file).
celery-redbeat>=2.2.0
# Static files served off the request workers (no Nginx sidecar on PaaS).
whitenoise>=6.7.0

# --- Observability ---
prometheus-client>=0.20.0
sentry-sdk[flask]>=2.10.0

# --- Features ---
qrcode[pil]>=7.4.2
razorpay>=1.4.2
# ANPR (license-plate recognition). Needs the `tesseract-ocr` system binary (in Dockerfile).
opencv-python-headless>=4.10.0
pytesseract>=0.3.10
numpy>=1.26.0

# --- Testing / load ---
pytest>=8.3.2
pytest-cov>=5.0.0
testcontainers[postgres]>=4.0.0
locust>=2.40.0
